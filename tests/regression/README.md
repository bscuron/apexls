# Regression corpus

One file per fixed bug, with a comment linking to the issue/PR that fixed
it. Entries are never deleted, even after refactors — this directory is the
permanent record that a past bug stays fixed.
